# Format: "alias|cluster-name|cloud|profile|region"
# cloud must match a _cloud_login_<cloud> / _cloud_kubeconfig_<cloud> function pair.
# Discovered EKS clusters — 2026-06-01T11:20:48Z
CLUSTERS=(
    "dev|dev-cloud-cluster-name|aws|devprofile|us-east-1"
    "myRandomCustomClusterConfig||custom||us-east-1" # currently, 'custom' just prevents scripts from clobbering manual configs
)
